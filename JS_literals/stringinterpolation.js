const price = 100;
const tax = 0.10;
console.log(`The total price is $${price + (price * tax)}`);

function getDiscount(price){
    return price * 0.1; // Example discount rate of 20%
}
console.log(`The discounted price is: $${(price)}`);

const score = 85;
console.log(`Result: ${score>=60 ? 'pass' : 'Fail'});`);
